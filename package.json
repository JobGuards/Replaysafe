{
  "name": "replaysafe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\" \"pnpm --filter landing dev\"",
    "build": "pnpm -r --filter !example-payment-app build",
    "test": "pnpm -r test",
    "lint": "pnpm -r run --if-present check-lint",
    "ci": "pnpm build && pnpm test",
    "release": "pnpm build && changeset publish",
    "db:generate": "pnpm --filter @replaysafe/db build",
    "db:push": "pnpm --filter @replaysafe/db exec prisma db push"
  },
  "keywords": [],
  "author": "pallavi",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "concurrently": "^9.2.1"
  },
  "pnpm": {
    "overrides": {
      "react": "19.2.0",
      "react-dom": "19.2.0",
      "lodash": ">=4.18.0",
      "fast-uri": ">=3.1.2",
      "brace-expansion@>=4.0.0 <5.0.5": ">=5.0.5",
      "path-to-regexp": ">=8.4.0",
      "vite": ">=7.3.2",
      "picomatch": ">=4.0.4",
      "postcss": ">=8.5.10"
    }
  }
}